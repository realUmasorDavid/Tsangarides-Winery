# fly.toml app configuration file generated for tsangarides-winery on 2026-06-07T16:55:25Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'tsangarides-winery'
primary_region = 'ams'
console_command = '/code/manage.py shell'

[build]

[deploy]
  release_command = "sh -c 'python manage.py migrate --noinput && python manage.py collectstatic --noinput'"

[env]
  PORT = '8000'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256

[[statics]]
  guest_path = '/code/static'
  url_prefix = '/static/'
